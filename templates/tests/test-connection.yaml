apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-server.fullname" . }}-test-connection"
  labels:
    {{- include "backend-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
